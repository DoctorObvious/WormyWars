from settings import *
import pygame


class Level:

    def __init__(self, number, name, portals, walls):

        self.portals = []
        self.walls = []
        self.number = []
        self.name = []


class PortalAction:

    def __init__(self, old_direction, new_coord, new_direction)
        self.old_direction = old_direction
        self.new_coord = new_coord
        self.new_direction = new_direction
        

class PortalPoint:

    def __init__(self, coord, portal_action)

        self.coord = coord
        self.left = left
    
    


class Portal:



[ 40, 100



portals0

level0 = Level(0, "Here We Go!", portals0, [])
